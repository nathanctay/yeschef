# Backend — Scraper Service

Node.js 20 + TypeScript microservice. Uses Crawlee + Playwright to bypass Cloudflare challenges.

- Use `npm` (not Bun or pnpm)
- Run tests: `npm test`
- Build: `npm run build`
- Dev: `npm run dev`
